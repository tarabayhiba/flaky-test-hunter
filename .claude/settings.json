{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "c=$(jq -r '.tool_input.command'); case \"$c\" in *\"git commit\"*) cd \"$CLAUDE_PROJECT_DIR\" && env -u PYTHONPATH .venv/bin/pre-commit run || { echo \"pre-commit checks failed - fix before committing\" >&2; exit 2; } ;; esac",
            "statusMessage": "Running ruff + mypy before commit..."
          }
        ]
      }
    ]
  }
}
